# dependencies
/node_modules
**/node_modules/
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env
.env.local
.env.*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# vault keys — NEVER commit
/keys/
/.keys_vaults/
vault-config.json

# ide
.vscode/
.idea/

temp
# kdo
.kdo/

# rust / anchor (programs/)
# NOTE: Cargo.lock is INTENTIONALLY committed for programs — reproducible builds
# are a security requirement for on-chain programs (audit + solana-verify).
target/
**/target/
.anchor/
*.so
test-ledger/
.test-ledger/

# foundry (contracts/)
contracts/out/
contracts/cache/
contracts/broadcast/
contracts/lib/

# keepers / sdk build artifacts
keepers/**/dist/
sdk/dist/
tests-integration/dist/

# anchor idl output (commit the program's IDL only if explicitly intended)
programs/target/idl/
programs/target/types/
# fly
fly.toml

#todo
TODO.md
