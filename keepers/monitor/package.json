{
  "name": "@foundation/monitor",
  "version": "0.1.0",
  "private": true,
  "description": "Real-time vault health monitor — buffer, NAV staleness, TVL drops, invariant violations (ADR-004).",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target node",
    "start": "bun run dist/index.js",
    "test": "bun test"
  },
  "dependencies": {
    "@foundation/sdk": "workspace:*"
  }
}
