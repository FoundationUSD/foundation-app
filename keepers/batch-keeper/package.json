{
  "name": "@foundation/batch-keeper",
  "version": "0.1.0",
  "private": true,
  "description": "Daily 1PM UTC — drain_managed → CCTP V2 burn on Solana → mint on Ethereum → subscribeToSAID (ADR-004).",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target node",
    "start": "bun run dist/index.js",
    "test": "bun test"
  },
  "dependencies": {
    "@foundation/sdk": "workspace:*"
  }
}
